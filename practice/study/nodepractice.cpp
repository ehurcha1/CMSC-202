#include <iostream>
#include <string>
using namespace std;

struct Node {
  string num;
  Node *m_next;
};

int main() {
  Node *head = nullptr;

  Node *newNode = new Node;

  newNode->num = 1;
  newNode->m_next = num + 1;
}